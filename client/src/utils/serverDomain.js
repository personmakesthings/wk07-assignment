// const serverDomain = "http://localhost:8080" // OFFLINE DEVELOPMENT
const serverDomain = "https://wk07-assignment-server.onrender.com" // RENDER DEPLOYMENT
export default serverDomain

// No slash at the end of the URLs for legibility reasons when using string literals when referencing these variables